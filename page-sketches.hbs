{{!--
=============================================================================
SKETCHES/ART PAGE TEMPLATE
=============================================================================

Custom page template for the /sketches/ page.
Displays a gallery grid of all posts tagged with #art.

SETUP:
1. Create a page in Ghost with slug "sketches"
2. Add title and optional description (custom_excerpt)
3. Posts tagged with #art will appear in the gallery

LAYOUT:
- Responsive grid gallery
- Hover overlay shows title
- Square aspect ratio thumbnails

CSS: See assets/css/custom.css §12 (Sketches Gallery)
--}}
{{!< default}}

{{#post}}
<section class="sketches-page outer">
    <div class="inner">
        <header class="sketches-header">
            <h1 class="sketches-title">{{title}}</h1>
            {{#if custom_excerpt}}
                <p class="sketches-description">{{custom_excerpt}}</p>
            {{/if}}
        </header>
        
        <div class="sketches-gallery">
            {{#get "posts" filter="tag:hash-art" limit="all" include="tags"}}
                {{#foreach posts}}
                    <a href="{{url}}" class="sketch-item">
                        {{#if feature_image}}
                            <img 
                                src="{{img_url feature_image size="m"}}"
                                srcset="{{img_url feature_image size="s"}} 300w,
                                        {{img_url feature_image size="m"}} 600w,
                                        {{img_url feature_image size="l"}} 1000w"
                                sizes="(max-width: 600px) 100vw, (max-width: 1000px) 50vw, 33vw"
                                alt="{{title}}"
                                loading="lazy"
                            >
                        {{else}}
                            <div class="sketch-placeholder">
                                <span>{{title}}</span>
                            </div>
                        {{/if}}
                        <div class="sketch-overlay">
                            <span class="sketch-title">{{title}}</span>
                        </div>
                    </a>
                {{/foreach}}
            {{/get}}
        </div>
    </div>
</section>
{{/post}}
