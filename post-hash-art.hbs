{{!--
=============================================================================
ART POST TEMPLATE
=============================================================================

Template for visual/image-focused posts.
Triggered by adding the internal tag #art to a post.

LAYOUT:
- Full-width hero image (the artwork)
- Title and optional caption below
- Description block for artist notes
- Minimal footer with date and tags

USAGE:
1. Create a post in Ghost
2. Add internal tag: #art
3. Set feature image as the artwork
4. Add description in post body

CSS CLASSES (see assets/css/custom.css §12):
- .post-art: Article wrapper
- .art-hero: Full-width image container
- .art-description: Text block below image
--}}
{{!< default}}

{{#post}}
<article class="post post-art {{post_class}}">
    {{!-- Hero image - the focal point --}}
    {{#if feature_image}}
        <figure class="art-hero">
            <img 
                src="{{img_url feature_image size="xl"}}" 
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                loading="eager"
            >
        </figure>
    {{/if}}

    {{!-- Title and caption are optional --}}
    {{#if title}}
        <header class="art-header">
            <h1>{{title}}</h1>
        </header>
    {{/if}}

    {{#if feature_image_caption}}
        <p class="art-caption">{{feature_image_caption}}</p>
    {{/if}}

    {{!-- Body content (description) - optional --}}
    {{#if content}}
        <section class="art-description">
            {{content}}
        </section>
    {{/if}}

    {{!-- Notes section: uses excerpt field for artwork details --}}
    {{!-- Example: "Oil on canvas, 24×36 in, 2024, Brooklyn studio" --}}
    {{#if excerpt}}
        <aside class="art-notes">
            <h2>Notes</h2>
            <p>{{excerpt}}</p>
        </aside>
    {{/if}}

    {{!-- Minimal footer --}}
    <footer class="art-footer">
        <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMMM YYYY"}}</time>
        
        {{!-- Show only public tags --}}
        {{#if primary_tag}}
            <span class="art-tags">
                {{#foreach tags visibility="public"}}
                    <a href="{{url}}">{{name}}</a>{{#unless @last}}, {{/unless}}
                {{/foreach}}
            </span>
        {{/if}}
    </footer>
</article>
{{/post}}
