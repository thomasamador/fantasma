{{!--
=============================================================================
POST CARD PARTIAL
=============================================================================

Card component for displaying posts in listings.
Used in index.hbs, tag.hbs, author.hbs, and page templates.

STRUCTURE:
- Feature image (responsive with srcset)
- Primary tag label
- Title
- Excerpt (truncated to 30 words)
- Meta (date, reading time)

IMAGE SIZES:
- Uses Ghost's responsive image helpers
- srcset for 300w, 600w, 1000w
- Lazy loading enabled

CSS: See assets/css/custom.css §7 (Post Cards)
--}}
<article class="post-card {{post_class}}">
    {{#if feature_image}}
    <a class="post-card-image-link" href="{{url}}">
        <img 
            class="post-card-image"
            srcset="{{img_url feature_image size="s"}} 300w,
                    {{img_url feature_image size="m"}} 600w,
                    {{img_url feature_image size="l"}} 1000w"
            sizes="(max-width: 720px) 100vw, 720px"
            src="{{img_url feature_image size="m"}}"
            alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
            loading="lazy"
        >
    </a>
    {{/if}}
    
    <div class="post-card-content">
        <a class="post-card-content-link" href="{{url}}">
            <header class="post-card-header">
                {{#if primary_tag}}
                    <span class="post-card-primary-tag">{{primary_tag.name}}</span>
                {{/if}}
                <h2 class="post-card-title">{{title}}</h2>
            </header>
            {{#if excerpt}}
                <div class="post-card-excerpt">{{excerpt words="30"}}</div>
            {{/if}}
        </a>
        
        <footer class="post-card-meta">
            <time class="post-card-meta-date" datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMM D, YYYY"}}</time>
            {{#if reading_time}}
                <span class="post-card-meta-length">{{reading_time}} min read</span>
            {{/if}}
        </footer>
    </div>
</article>
