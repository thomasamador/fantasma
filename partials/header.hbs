{{!--
=============================================================================
SITE HEADER PARTIAL
=============================================================================

Site header with logo, navigation menu, and action buttons.
Included in default.hbs via {{> header}}

STRUCTURE:
- .gh-head-brand: Logo, mobile search, burger button
- .gh-head-menu: Navigation links (uses Ghost {{navigation}} helper)
- .gh-head-actions: Desktop search, Subscribe/Account button

MOBILE BEHAVIOR (≤767px):
- Hamburger button appears, hides desktop nav
- Click burger toggles .is-open class on header
- Full-screen nav overlay with centered links
- Search button visible next to burger
- Body scroll locked when menu open

NAVIGATION:
Links are managed in Ghost Admin → Settings → Navigation.
Uses the {{navigation}} helper which outputs <ul><li><a>...</a></li></ul>

MEMBERS:
- Shows "Subscribe" button if not logged in
- Shows "Account" button if logged in
- Uses Ghost Portal for signup/account management

CSS: See assets/css/custom.css §5 (Header & Navigation)
JS: See assets/js/main.js (burger toggle)
--}}
<header class="gh-head outer" id="gh-head">
    <div class="gh-head-inner inner">
        
        {{!-- Brand area: logo, mobile search, burger --}}
        <div class="gh-head-brand">
            <a class="gh-head-logo" href="{{@site.url}}">
                {{#if @site.logo}}
                    <img src="{{@site.logo}}" alt="{{@site.title}}">
                {{else}}
                    {{@site.title}}
                {{/if}}
            </a>
            
            {{!-- Mobile search (visible only on mobile) --}}
            <button class="gh-search gh-search-mobile" aria-label="Search this site" data-ghost-search>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" width="20" height="20">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
            </button>
            
            {{!-- Hamburger button (visible only on mobile) --}}
            <button class="gh-burger" aria-label="Menu">
                {{!-- Burger icon (3 lines) --}}
                <svg class="gh-burger-open" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 256 256">
                    <path d="M224,128a8,8,0,0,1-8,8H40a8,8,0,0,1,0-16H216A8,8,0,0,1,224,128ZM40,72H216a8,8,0,0,0,0-16H40a8,8,0,0,0,0,16ZM216,184H40a8,8,0,0,0,0,16H216a8,8,0,0,0,0-16Z"></path>
                </svg>
                {{!-- Close icon (X) --}}
                <svg class="gh-burger-close" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 256 256">
                    <path d="M205.66,194.34a8,8,0,0,1-11.32,11.32L128,139.31,61.66,205.66a8,8,0,0,1-11.32-11.32L116.69,128,50.34,61.66A8,8,0,0,1,61.66,50.34L128,116.69l66.34-66.35a8,8,0,0,1,11.32,11.32L139.31,128Z"></path>
                </svg>
            </button>
        </div>
        
        {{!-- Navigation menu --}}
        <nav class="gh-head-menu">
            {{navigation}}
        </nav>
        
        {{!-- Actions: search (desktop), subscribe/account --}}
        <div class="gh-head-actions">
            {{!-- Desktop search (hidden on mobile) --}}
            <button class="gh-search gh-search-desktop" aria-label="Search this site" data-ghost-search>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" width="18" height="18">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
            </button>
            {{#if @site.members_enabled}}
                {{#unless @member}}
                    <a class="gh-head-button" href="#/portal/signup" data-portal="signup">Subscribe</a>
                {{else}}
                    <a class="gh-head-button" href="#/portal/account" data-portal="account">Account</a>
                {{/unless}}
            {{/if}}
        </div>
        
    </div>
</header>
