{{!-- Essay card: a long-form post card with optional feature image, title, excerpt, and metadata --}}
<article class="gh-card gh-card-essay {{post_class}}{{#unless @custom.show_images_in_feed}} no-image{{/unless}}" data-post-type="essay" data-date="{{date format="X"}}">
    <a class="gh-card-link" href="{{url}}">
        {{!-- Feature image: responsive WebP srcset across 6 breakpoints, only rendered when the post has an image --}}
        {{#if feature_image}}
            <figure class="gh-card-image">
                <img
                    srcset="{{img_url feature_image size="xs" format="webp"}} 160w,
                            {{img_url feature_image size="s" format="webp"}} 320w,
                            {{img_url feature_image size="m" format="webp"}} 600w,
                            {{img_url feature_image size="l" format="webp"}} 960w,
                            {{img_url feature_image size="xl" format="webp"}} 1200w,
                            {{img_url feature_image size="xxl" format="webp"}} 2000w"
                    sizes="{{#if imageSizes}}{{imageSizes}}{{else}}320px{{/if}}"
                    src="{{img_url feature_image size="m"}}"
                    alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                    {{#if lazyLoad}}loading="lazy"{{/if}}
                >
            </figure>
        {{/if}}
        {{!-- Card body: "Essay" type label, post title, and an optional excerpt --}}
        <div class="gh-card-wrapper">
            <p class="gh-card-essay-label">Essay</p>
            <h3 class="gh-card-title is-title">{{title}}</h3>
            {{!-- Excerpt: prefer the author's custom excerpt; fall back to the auto-generated excerpt --}}
            {{#if custom_excerpt}}
                <p class="gh-card-excerpt is-body">{{custom_excerpt}}</p>
            {{else if excerpt}}
                <p class="gh-card-excerpt is-body">{{excerpt}}</p>
            {{/if}}
            {{!-- Footer metadata: lock icon for members-only posts, reading time, and conditional publish date --}}
            <footer class="gh-card-meta">
                {{#unless access}}
                    {{^has visibility="public"}}
                        {{> "icons/lock"}}
                    {{/has}}
                {{/unless}}
                {{#if reading_time}}
                    <span class="gh-card-reading-time">{{reading_time}}</span>
                {{/if}}
                {{#if @custom.show_publish_date}}
                    <time class="gh-card-date" datetime="{{date format="YYYY-MM-DD"}}">{{date format="DD MMM YYYY"}}</time>
                {{/if}}
            </footer>
        </div>
    </a>
</article>
