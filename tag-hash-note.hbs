{{!--
=============================================================================
NOTE TAG ARCHIVE TEMPLATE
=============================================================================

Stream layout for posts tagged with #note.
Automatically used when visiting /tag/hash-note/

LAYOUT:
- Header with tag name and post count
- Timeline-style stream of notes

NOTE: Users typically access notes via /notes/ page (page-notes.hbs)
rather than this tag archive directly.

CSS: See assets/css/custom.css §12 (Notes Stream)
--}}
{{!< default}}

<header class="archive-header notes-header">
    <h1>Notes</h1>
    {{#if tag.description}}
        <p>{{tag.description}}</p>
    {{/if}}
    <small>{{plural tag.count.posts empty="No notes" singular="% note" plural="% notes"}}</small>
</header>

<section class="notes-stream">
    {{#foreach posts}}
        <article class="note-card">
            <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMMM D, YYYY"}}</time>
            
            {{#if title}}
                <h2><a href="{{url}}">{{title}}</a></h2>
            {{/if}}
            
            <div class="note-card-content">
                {{#if excerpt}}
                    <p>{{excerpt words="50"}}</p>
                {{/if}}
            </div>
            
            <a href="{{url}}" class="note-card-link">Read note →</a>
        </article>
    {{/foreach}}
</section>

{{> pagination}}
