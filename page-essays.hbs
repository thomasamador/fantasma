{{!--
=============================================================================
ESSAYS PAGE TEMPLATE
=============================================================================

Custom page template for the /essays/ page.
Displays a list of all posts tagged with #essay.

SETUP:
1. Create a page in Ghost with slug "essays"
2. Add title and optional description (custom_excerpt)
3. Posts tagged with #essay will be listed automatically

USES:
- {{get "posts" filter="tag:hash-essay"}} to fetch essay posts
- Shows title, excerpt, date, and reading time

CSS: See assets/css/custom.css §12 (Essays Listing)
--}}
{{!< default}}

{{#post}}
<section class="essays-page outer">
    <div class="inner">
        <header class="essays-header">
            <h1 class="essays-title">{{title}}</h1>
            {{#if custom_excerpt}}
                <p class="essays-description">{{custom_excerpt}}</p>
            {{/if}}
        </header>
        
        <div class="essays-list">
            {{#get "posts" filter="tag:hash-essay" limit="all" include="tags"}}
                {{#foreach posts}}
                    <article class="essay-item">
                        <a href="{{url}}" class="essay-link">
                            <h2 class="essay-item-title">{{title}}</h2>
                            {{#if excerpt}}
                                <p class="essay-item-excerpt">{{excerpt words="30"}}</p>
                            {{/if}}
                            <div class="essay-item-meta">
                                <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMM D, YYYY"}}</time>
                                {{#if reading_time}}
                                    <span class="reading-time">{{reading_time}} min read</span>
                                {{/if}}
                            </div>
                        </a>
                    </article>
                {{/foreach}}
            {{/get}}
        </div>
    </div>
</section>
{{/post}}
